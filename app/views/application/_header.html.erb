<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Brand", root_path, class: "navbar-brand" %>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <div class="navbar-right">
        <% if user_signed_in? %>
          <%= link_to "Go to dashboard", dashboard_path, class: 'btn btn-link navbar-btn' %>
          <%= link_to destroy_user_session_path, method: "delete", class: 'btn btn-logout navbar-btn' do %>
            <span class="glyphicon glyphicon-log-out" aria-hidden="true"></span>
          <% end %>
        <% else %>
          <%= link_to "Login", login_path, class: 'btn btn-default btn-sm navbar-btn' %>
          <%= link_to "New Account", register_path, class: 'btn btn-primary btn-sm navbar-btn' %>
        <% end %>
      </div>
    </div>
  </div>
</nav>
